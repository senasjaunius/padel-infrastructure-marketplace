export default async function handler(req,res){
 if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const base=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,adminKey=process.env.ADMIN_DASHBOARD_KEY;if(!adminKey||req.headers['x-admin-key']!==adminKey)return res.status(401).json({error:'Unauthorized'});if(!base||!key)return res.status(500).json({error:'Supabase environment variables are missing'});try{const h={apikey:key,Authorization:`Bearer ${key}`};const r=await fetch(`${base}/rest/v1/supplier_profiles?select=*&order=created_at.desc`,{headers:h});const t=await r.text();if(!r.ok)return res.status(r.status).send(t);return res.status(200).send(t)}catch(e){return res.status(500).json({error:e?.message||'Unable to load suppliers'})}}
