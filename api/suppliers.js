export default async function handler(req,res){
 if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});
 const base=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;
 if(!base||!key)return res.status(500).json({error:'Supabase environment variables are missing'});
 try{const b=typeof req.body==='string'?JSON.parse(req.body):req.body;const company_name=String(b.company_name||'').trim(),email=String(b.email||'').trim(),country=String(b.country||'').trim();if(!company_name||!email||!country)return res.status(400).json({error:'Company, email and country are required'});const services=Array.isArray(b.services)?b.services.map(String):[];const payload={company_name,contact_name:String(b.contact_name||'').trim()||null,email,country,city:String(b.city||'').trim()||null,website:String(b.website||'').trim()||null,phone:String(b.phone||'').trim()||null,services,service_area:String(b.service_area||'').trim()||null,description:String(b.description||'').trim()||null};const h={apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json',Prefer:'return=minimal'};const r=await fetch(`${base}/rest/v1/supplier_profiles`,{method:'POST',headers:h,body:JSON.stringify(payload)});const t=await r.text();if(!r.ok)return res.status(r.status).send(t);return res.status(201).json({ok:true});}catch(e){return res.status(400).json({error:e?.message||'Invalid request'})}}
